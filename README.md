# ResumeExtractor
Parameter-efficient fine-tuning (LoRA) of Llama-3.2-3B for automated resume parsing. Includes dataset preprocessing, training pipeline, LoRA adapters, and inference demo for extracting structured JSON fields from raw resumes.
